contract:
  name: "bench_stat_disloc_confirmatory"
  output_mode: "event_detector"
  observation_clock: "bar_close"
  decision_lag_bars: 1
  lookback_bars: 288
  uses_current_observation: false
  calibration_mode: "train_fit"
  fit_scope: "split_train_only"
  approved_primitives:
    - "basis_zscore"
    - "funding_rate_scaled"
  notes: >
    Maintained confirmatory contract for STATISTICAL_DISLOCATION family.
