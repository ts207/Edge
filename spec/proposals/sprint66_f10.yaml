# Sprint 6.6 — F10: BASIS_DISLOC + basis_repair + long
# Spot-perp basis dislocations always repair — arbitrage enforces convergence.
# Basis dislocation to downside (perp discount) → basis repair = long perp.
# Very high-certainty effect; basis data is in core feature files.
program_id: sprint66_matrix_f10
start: "2021-01-01"
end: "2024-10-31"
symbols:
  - BTCUSDT
timeframe: 5m
hypothesis:
  anchor:
    type: event
    event_id: BASIS_DISLOC
  filters:
    states: []
    regimes: []
    feature_predicates: []
    contexts: {}
  sampling_policy:
    mode: episodic
    entry_lag_bars: 1
    overlap_policy: suppress
  template:
    id: basis_repair
  direction: long
  horizon_bars: 12
instrument_classes:
  - crypto
  - futures
objective_name: retail_profitability
promotion_profile: research
