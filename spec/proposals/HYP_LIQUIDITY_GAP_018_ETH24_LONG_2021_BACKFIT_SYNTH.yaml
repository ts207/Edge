# HYP_LIQUIDITY_GAP_018_ETH24_LONG_2021_BACKFIT_SYNTH - ETH LIQUIDITY_GAP_PRINT 24b long 2021 synthetic backfit
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-02
#
# Backfit note: this proposal intentionally tunes the search/gate threshold
# profiles to the repo's synthetic/exploratory settings for 2021-only research.
# It is not a promotion-valid claim; it is a bounded in-sample calibration run.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: disabled
run_mode: research
discovery_profile: synthetic
phase2_gate_profile: synthetic

description: >
  ETH-only 2021 synthetic backfit isolate for LIQUIDITY_GAP_PRINT continuation
  (HYP_LIQUIDITY_GAP_018_ETH24_LONG_2021_BACKFIT_SYNTH). The broad 2021
  liquidity-stress sweep showed the strongest regime-conditional near-miss at
  ETHUSDT / LIQUIDITY_GAP_PRINT / long / 24 bars. This run freezes that exact
  branch and tunes only the search/gate threshold profiles to the repo's
  synthetic exploratory settings.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2021-01-01"
end: "2021-12-31"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 24

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
