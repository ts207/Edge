# HYP_FAILED_CONT_020_BTC48_LONG_UPDIR_FPOS_HIGH_2024Q3 - BTC FAILED_CONTINUATION bridge confirmation extension through 2024 Q3
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: the aggressive recalibration isolated one real research survivor:
# up-direction FAILED_CONTINUATION in funding-positive high-volatility context,
# traded long over 48 bars. The next bounded question is no longer trigger
# tuning; it is whether adjacent 2024 Q2-Q3 data adds enough support to stop the
# branch from failing bridge OOS/regime validation.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  BTC-only bridge confirmation extension for FAILED_CONTINUATION
  (HYP_FAILED_CONT_020_BTC48_LONG_UPDIR_FPOS_HIGH_2024Q3). This run freezes the
  exact 48-bar long survivor from the aggressive retune and changes only the end
  date from 2024-04-01 to 2024-10-01 to test whether adjacent 2024 Q2-Q3 data
  improves OOS and regime-stability support.

symbols:
  - BTCUSDT

timeframe: 5m
start: "2022-01-01"
end: "2024-10-01"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - event_id: FAILED_CONTINUATION
        event_direction: up

templates:
  - exhaustion_reversal

horizons_bars:
  - 24
  - 48

directions:
  - long

entry_lags:
  - 1

contexts:
  carry_state:
    - funding_pos
  vol_regime:
    - high

search_control:
  max_hypotheses_total: 5
  max_hypotheses_per_template: 5
  max_hypotheses_per_event_family: 5

bounded:
  baseline_run_id: prog_fresh_start_2026q2_20260402T022559Z_70aa61250a
  experiment_type: confirmation
  allowed_change_field: end
  change_reason: Extend the exact 48b tuned survivor into adjacent 2024 Q2-Q3 for bridge-focused OOS confirmation.
  compare_to_baseline: true

discovery_profile: standard
phase2_gate_profile: discovery
