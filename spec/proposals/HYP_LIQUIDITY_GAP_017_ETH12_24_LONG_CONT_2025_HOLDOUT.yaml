# HYP_LIQUIDITY_GAP_017_ETH12_24_LONG_CONT_2025_HOLDOUT - ETH LIQUIDITY_GAP_PRINT 12b/24b continuation 2025 holdout
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-02
#
# Mechanism: This is the explicit calendar holdout for the ETH
# LIQUIDITY_GAP_PRINT continuation branch after the 2022-2024 train/test run.
# It keeps the exact same event family, regime, symbol, direction, template,
# horizons, and entry timing and changes only the date window to 2025.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only 2025 calendar holdout for LIQUIDITY_GAP_PRINT continuation
  (HYP_LIQUIDITY_GAP_017_ETH12_24_LONG_CONT_2025_HOLDOUT). This run validates
  the same ETHUSDT / long / LIQUIDITY_STRESS continuation mechanism on the
  2025 out-of-sample holdout after the 2022-2024 train/test window.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2025-01-01"
end: "2026-01-01"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 12
  - 24

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
