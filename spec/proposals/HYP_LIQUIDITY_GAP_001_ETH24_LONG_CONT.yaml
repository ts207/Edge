# HYP_LIQUIDITY_GAP_001_ETH24_LONG_CONT - ETH LIQUIDITY_GAP_PRINT 24b continuation
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: The corrected broad LIQUIDITY_DISLOCATION rerun produced one localized
# pre-multiplicity pocket on ETH: LIQUIDITY_GAP_PRINT followed by 24-bar long
# continuation. This proposal isolates that exact cell to test whether the effect
# survives as a bounded single-expression search when broad-family multiplicity and
# unrelated event surfaces are removed.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only LIQUIDITY_GAP_PRINT continuation isolate
  (HYP_LIQUIDITY_GAP_001_ETH24_LONG_CONT). The corrected broad 2022-2023
  liquidity-dislocation rerun eliminated the prior SPREAD_BLOWOUT label drift and
  showed that the only remaining pocket is ETHUSDT, long, 24-bar continuation
  after LIQUIDITY_GAP_PRINT. This run tests that exact bounded expression without
  broad-family multiplicity dilution.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2022-01-01"
end: "2023-12-31"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 24

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
