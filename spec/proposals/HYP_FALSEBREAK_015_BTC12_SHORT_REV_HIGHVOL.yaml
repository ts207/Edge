# HYP_FALSEBREAK_015_BTC12_SHORT_REV_HIGHVOL - BTC FALSE_BREAKOUT 12b short high-vol retune
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: the unconditioned FALSE_BREAKOUT event-surface retune produced valid
# event hits but weak expectancy and t-stat. This follow-up keeps the exact event,
# template, direction, horizon, and time window fixed and changes only the market
# context to vol_regime=high to test whether the short reversal is denser and more
# stable inside stressed volatility.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  BTC-only event-surface isolate for FALSE_BREAKOUT with high-volatility context
  (HYP_FALSEBREAK_015_BTC12_SHORT_REV_HIGHVOL). This run freezes the bounded
  FALSE_BREAKOUT short 12-bar reversal expression from the prior event-surface
  retune and changes only vol_regime=high.

symbols:
  - BTCUSDT

timeframe: 5m
start: "2022-01-01"
end: "2024-04-01"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - FALSE_BREAKOUT

templates:
  - false_breakout_reversal

horizons_bars:
  - 12

directions:
  - short

entry_lags:
  - 1

contexts:
  vol_regime:
    - high

search_control:
  max_hypotheses_total: 5
  max_hypotheses_per_template: 5
  max_hypotheses_per_event_family: 5

bounded:
  baseline_run_id: prog_fresh_start_2026q2_20260402T014615Z_64aa6437f3
  experiment_type: regime_test
  allowed_change_field: contexts
  change_reason: Restrict the calibrated false-breakout branch to high-volatility context only.
  compare_to_baseline: true

discovery_profile: standard
phase2_gate_profile: discovery
