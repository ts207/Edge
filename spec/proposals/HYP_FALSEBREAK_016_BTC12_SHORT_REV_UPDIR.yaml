# HYP_FALSEBREAK_016_BTC12_SHORT_REV_UPDIR - BTC FALSE_BREAKOUT 12b short retune with up-direction events only
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: event-level retuning on the calibrated false-breakout surface shows
# that the productive branch is not a volatility slice but the event-side split:
# upward false breakouts support the contrarian short response, while downward
# false breakouts do not. This proposal keeps the event-surface expression fixed
# and changes only the trigger to upward FALSE_BREAKOUT events.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  BTC-only event-surface isolate for FALSE_BREAKOUT with upward event-direction
  filtering (HYP_FALSEBREAK_016_BTC12_SHORT_REV_UPDIR). This run freezes the
  short 12-bar false-breakout reversal expression from the calibrated event
  surface and restricts the trigger to up-direction FALSE_BREAKOUT events only.

symbols:
  - BTCUSDT

timeframe: 5m
start: "2022-01-01"
end: "2024-04-01"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - event_id: FALSE_BREAKOUT
        event_direction: up

templates:
  - false_breakout_reversal

horizons_bars:
  - 12

directions:
  - short

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 5
  max_hypotheses_per_template: 5
  max_hypotheses_per_event_family: 5

bounded:
  baseline_run_id: prog_fresh_start_2026q2_20260402T014615Z_64aa6437f3
  experiment_type: confirmation
  allowed_change_field: trigger_space
  change_reason: Restrict the calibrated false-breakout branch to up-direction event hits only.
  compare_to_baseline: true

discovery_profile: standard
phase2_gate_profile: discovery
