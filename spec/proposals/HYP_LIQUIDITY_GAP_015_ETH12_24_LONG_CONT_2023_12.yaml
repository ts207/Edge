# HYP_LIQUIDITY_GAP_015_ETH12_24_LONG_CONT_2023_12 - ETH LIQUIDITY_GAP_PRINT 12b/24b continuation 2023-12
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-02
#
# Mechanism: The ETH LIQUIDITY_GAP_PRINT continuation branch is strongest in
# December 2023, where the 24-bar expression already survives as a final bridge
# candidate. This proposal keeps the exact same event family, regime, symbol,
# direction, and December window, and changes one bounded axis only: test 12-bar
# and 24-bar continuation together to determine whether the strongest month also
# supports a second survivor at the shorter horizon.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only December 2023 LIQUIDITY_GAP_PRINT continuation multi-horizon retune
  (HYP_LIQUIDITY_GAP_015_ETH12_24_LONG_CONT_2023_12). The December 2023 slice is
  the strongest bounded concentration of the ETH liquidity-gap continuation
  mechanism. This run freezes the same ETHUSDT / long / LIQUIDITY_STRESS setup
  and tests whether both 12-bar and 24-bar continuation survive inside the
  strongest month.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2023-12-01"
end: "2024-01-01"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 12
  - 24

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
