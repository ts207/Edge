# HYP_LIQUIDITY_GAP_002_ETH24_LONG_CONT_HIGHVOL - ETH LIQUIDITY_GAP_PRINT 24b continuation high-vol
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: The isolated ETH LIQUIDITY_GAP_PRINT 24-bar continuation cell survives
# search and multiplicity but fails bridge robustness. This follow-up changes one
# variable only: require high volatility context, matching the repo's maintained
# LIQUIDITY_GAP_PRINT benchmark context and aiming to improve OOS stability.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only LIQUIDITY_GAP_PRINT continuation isolate with high-volatility context
  (HYP_LIQUIDITY_GAP_002_ETH24_LONG_CONT_HIGHVOL). The parent isolate confirmed a
  real search-level discovery at ETHUSDT / long / 24 bars, but bridge robustness
  remained sub-threshold. This run keeps the exact same expression and adds only
  vol_regime=high to test whether the signal becomes more stable inside stressed
  volatility conditions already recognized by the repo's benchmark surface.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2022-01-01"
end: "2023-12-31"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 24

directions:
  - long

entry_lags:
  - 1

contexts:
  vol_regime:
    - high

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
