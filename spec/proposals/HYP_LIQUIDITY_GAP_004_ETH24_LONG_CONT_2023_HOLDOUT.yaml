# HYP_LIQUIDITY_GAP_004_ETH24_LONG_CONT_2023_HOLDOUT - ETH LIQUIDITY_GAP_PRINT 24b continuation 2023 holdout
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: The base ETH LIQUIDITY_GAP_PRINT 24-bar continuation isolate
# remained the strongest branch in LIQUIDITY_STRESS, while later refinements
# weakened the line. This holdout keeps the base expression intact and tests
# whether it survives in the 2023 calendar slice.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only 2023 temporal holdout for LIQUIDITY_GAP_PRINT continuation
  (HYP_LIQUIDITY_GAP_004_ETH24_LONG_CONT_2023_HOLDOUT). The parent ETH isolate
  remained the strongest bounded liquidity-gap branch after later refinements
  weakened. This run freezes the same event, regime, template, horizon,
  direction, and entry timing while narrowing the date window to 2023 only to
  test whether the edge survives as a bounded calendar holdout.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2023-01-01"
end: "2023-12-31"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 24

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
