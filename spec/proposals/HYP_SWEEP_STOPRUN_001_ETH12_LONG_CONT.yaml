# HYP_SWEEP_STOPRUN_001_ETH12_LONG_CONT - ETH SWEEP_STOPRUN 12b continuation
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: After exhausting the LIQUIDITY_GAP_PRINT continuation branch, the
# next strongest corrected near-miss in the broad 2022-2023 LIQUIDITY_DISLOCATION
# rerun was ETH SWEEP_STOPRUN long continuation at 12 bars. This proposal isolates
# that exact supported template/expression.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only SWEEP_STOPRUN continuation isolate
  (HYP_SWEEP_STOPRUN_001_ETH12_LONG_CONT). The corrected broad 2022-2023
  liquidity-dislocation rerun showed SWEEP_STOPRUN / long / 12 bars as the next
  strongest local near-miss after the exhausted LIQUIDITY_GAP_PRINT branch. This
  run tests that exact bounded expression with the event's supported continuation
  template.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2022-01-01"
end: "2023-12-31"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - SWEEP_STOPRUN
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 12

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
