# HYP_LIQUIDITY_GAP_014_ETH12_24_LONG_CONT_2023_H2 - ETH LIQUIDITY_GAP_PRINT 12b/24b continuation 2023 H2
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-02
#
# Mechanism: The ETH 2023 LIQUIDITY_GAP_PRINT continuation branch is concentrated
# in H2, and the 24-bar expression already survives there. The broad 2022-2023
# family run also showed a weaker but positive 12-bar branch. This proposal keeps
# the exact same event family, regime, symbol, direction, and H2 window, and
# changes one bounded axis only: test 12-bar and 24-bar continuation together to
# see whether the concentrated 2023 H2 slice supports multiple survivors.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only 2023 H2 LIQUIDITY_GAP_PRINT continuation multi-horizon retune
  (HYP_LIQUIDITY_GAP_014_ETH12_24_LONG_CONT_2023_H2). The 2023 H2 branch
  survives on the 24-bar continuation expression while 2022 and 2023 H1 do not.
  This run freezes the same ETHUSDT / long / LIQUIDITY_STRESS mechanism and
  tests whether the concentrated H2 slice supports both 12-bar and 24-bar
  continuation without changing event family, template family, or context axes.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2023-07-01"
end: "2024-01-01"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 12
  - 24

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
