# HYP_LIQUIDITY_GAP_005_ETH24_LONG_CONT_2022_MIRROR - ETH LIQUIDITY_GAP_PRINT 24b continuation 2022 mirror
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: The 2023 ETH LIQUIDITY_GAP_PRINT holdout strengthened the base
# 24-bar long continuation isolate. This mirror keeps the same bounded
# expression and tests whether the adjacent 2022 slice supports the same signal.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only 2022 temporal mirror for LIQUIDITY_GAP_PRINT continuation
  (HYP_LIQUIDITY_GAP_005_ETH24_LONG_CONT_2022_MIRROR). The 2023 holdout
  strengthened the base ETH 24-bar long continuation signal. This follow-up
  freezes the same event, regime, template, horizon, direction, and entry
  timing while narrowing the date window to 2022 only to test whether the
  mechanism is stable across the adjacent calendar slice.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2022-01-01"
end: "2022-12-31"

trigger_space:
  allowed_trigger_types: [EVENT]
  events:
    include:
      - LIQUIDITY_GAP_PRINT
  canonical_regimes:
    - LIQUIDITY_STRESS

templates:
  - continuation

horizons_bars:
  - 24

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10
