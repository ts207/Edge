# HYP_SEQ_LIQ_RECOVERY_001_ETH12_LONG_MR - ETH liquidity vacuum then recovery 12b long mean reversion
# Program: PROG_FRESH_START_2026Q2 | Author: Codex | Date: 2026-04-01
#
# Mechanism: After exhausting the sparse BTC funding-breakout sequence line, move
# to a denser stress/recovery family. This probe tests whether ETH
# LIQUIDITY_VACUUM followed by VOL_RELAXATION_START produces a short-horizon long
# mean-reversion pocket under discovery gates.

program_id: PROG_FRESH_START_2026Q2
objective_name: retail_profitability
promotion_profile: research
run_mode: research

description: >
  ETH-only sequence isolate for liquidity vacuum then recovery
  (HYP_SEQ_LIQ_RECOVERY_001_ETH12_LONG_MR). This run tests whether the
  LIQUIDITY_VACUUM -> VOL_RELAXATION_START chain on ETHUSDT supports a 12-bar
  long mean-reversion response when evaluated under discovery tuning.

symbols:
  - ETHUSDT

timeframe: 5m
start: "2022-01-01"
end: "2023-12-31"

trigger_space:
  allowed_trigger_types: [SEQUENCE]
  sequences:
    include:
      - [LIQUIDITY_VACUUM, VOL_RELAXATION_START]
    max_gaps_bars: [48]

templates:
  - mean_reversion

horizons_bars:
  - 12

directions:
  - long

entry_lags:
  - 1

contexts: {}

search_control:
  max_hypotheses_total: 10
  max_hypotheses_per_template: 10
  max_hypotheses_per_event_family: 10

discovery_profile: standard
phase2_gate_profile: discovery
