version: 1
kind: event
event_type: SEQ_LIQ_VACUUM_THEN_DEPTH_RECOVERY
active: true
synthetic_coverage: covered
canonical_family: LIQUIDITY_DISLOCATION
reports_dir: event_sequences
events_file: event_sequences.parquet
signal_column: seq_liq_vacuum_then_depth_recovery_event
parameters:
  max_gap_bars: 48
  cooldown_bars: 24
trigger: Detector triggers on seq liq vacuum then depth recovery pattern
confirmation: Post-detection price action confirms signal validity
expected_behavior:
  holding_horizon_bars: 1-12
  invalidation_conditions:
  - Volume returns above median within 1-2 bars
  - Spread compresses back to baseline
  disabled_regimes:
  - 'SPREAD_BLOWOUT active: spread elevated, unreliable signal'
cluster_id: liquidity_stress
