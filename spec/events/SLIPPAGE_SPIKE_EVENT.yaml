event_type: SLIPPAGE_SPIKE_EVENT
synthetic_coverage: covered
reports_dir: execution_friction
events_file: execution_friction_events.parquet
signal_column: slippage_spike_event
parameters:
  spread_z_threshold: 3.0
  slippage_z_threshold: 3.0
  cooldown_bars: 12
  merge_gap_bars: 0
canonical_family: EXECUTION_FRICTION
trigger: Detector triggers on slippage spike event pattern
confirmation: Post-detection price action confirms signal validity
expected_behavior:
  holding_horizon_bars: 0-4
  invalidation_conditions:
  - Fees return to baseline
  - Slippage normalizes
  disabled_regimes:
  - 'Market closed: execution data unavailable'
cluster_id: execution_cost
