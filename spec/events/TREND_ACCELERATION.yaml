event_type: TREND_ACCELERATION
synthetic_coverage: covered
reports_dir: trend_structure
events_file: trend_structure_events.parquet
signal_column: trend_acceleration_event
parameters:
  trend_window: 96
  breakout_z_threshold: 2.5
  min_spacing: 192
  merge_gap_bars: 0
canonical_family: TREND_STRUCTURE
trigger: Detector triggers on trend acceleration pattern
confirmation: Post-detection price action confirms signal validity
expected_behavior:
  holding_horizon_bars: 12-48
  invalidation_conditions:
  - Trend reverses within holding period
  - Range expands significantly beyond breakout level
  disabled_regimes:
  - 'Chop regime: no clear trend direction'
cluster_id: trend_momentum
