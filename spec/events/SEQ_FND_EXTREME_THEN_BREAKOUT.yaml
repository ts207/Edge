version: 1
kind: event
event_type: SEQ_FND_EXTREME_THEN_BREAKOUT
active: true
synthetic_coverage: covered
canonical_family: POSITIONING_EXTREMES
reports_dir: event_sequences
events_file: event_sequences.parquet
signal_column: seq_fnd_extreme_then_breakout_event
parameters:
  max_gap_bars: 96
  cooldown_bars: 24
trigger: Detector triggers on seq fnd extreme then breakout pattern
confirmation: Post-detection price action confirms signal validity
expected_behavior:
  holding_horizon_bars: 4-24
  invalidation_conditions:
  - Position unwinds within expected horizon
  - Funding reverses sign
  disabled_regimes:
  - 'Low liquidity regime: OI/vol signals unreliable'
cluster_id: funding_crowding
