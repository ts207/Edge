event_type: INT_ZSCORE_OVERSHOOT_CONFIRM
synthetic_coverage: experimental
description: "ZSCORE_STRETCH confirmed by OVERSHOOT_AFTER_SHOCK within 12 bars — dislocation then overshoot"
parameters:
  left_event: ZSCORE_STRETCH
  right_event: OVERSHOOT_AFTER_SHOCK
  op: confirm
  max_gap_bars: 12
canonical_family: STATISTICAL_DISLOCATION