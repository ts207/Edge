version: 1
kind: search_spec
metadata:
  phase: "full"
  description: "Maximum coverage: all ontology-defined searchable families."
search_mode: full

triggers:
  event_families: "*" # expand all ontology-defined event families marked searchable
  state_families: "*" # expand all ontology-defined state families marked searchable

contexts:
  vol_regime: "*"
  carry_state: "*"
  ms_trend_state: "*"
  ms_spread_state: "*"

templates: "*"
horizons: [5m, 15m, 60m]
directions: [long, short]
entry_lag: "*"
cost_profiles: [standard]
