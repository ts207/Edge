version: 1
kind: search_spec
metadata:
  phase: benchmark
  description: "Narrow maintained benchmark search spec for FALSE_BREAKOUT context comparison."
search_mode: subset

triggers:
  events:
    - FALSE_BREAKOUT

contexts:
  vol_regime: high

templates:
  - mean_reversion
horizons:
  - 15m
directions:
  - long
  - short
entry_lag:
  - 1
cost_profiles:
  - standard
