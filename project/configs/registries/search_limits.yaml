version: 1

limits:
  max_events_per_run: 12
  max_templates_per_run: 6
  max_horizons_per_run: 5
  max_directions_per_run: 2
  max_entry_lags_per_run: 4
  max_sequence_length: 3
  max_sequence_gap_values_per_run: 3
  max_context_slices_per_run: 8
  max_hypotheses_total: 1000
  max_hypotheses_per_template: 250
  max_hypotheses_per_event_family: 300

defaults:
  horizons_bars: [1, 3, 12, 24, 72] # 5m, 15m, 1h, 2h, 6h
  directions: [long, short]
  entry_lags: [0, 1, 2]
